{% extends 'base.html.twig' %}

{% block title %}Project{% endblock %}

{% block body %}
    <h1>Project</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project.description }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ project.creationDate ? project.creationDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DueDate</th>
                <td>{{ project.dueDate ? project.dueDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>IsArchived</th>
                <td>{{ project.isArchived ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('project_index') }}">back to list</a>

    <a href="{{ path('project_edit', {'id': project.id}) }}">edit</a>

    {{ include('project/_delete_form.html.twig') }}
{% endblock %}
