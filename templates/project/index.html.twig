{% extends 'base.html.twig' %}

{% block title %}Project index{% endblock %}

{% block mainTitle %}Vos projets{% endblock %}

{% block body %}
    <section class="row">
      {% for project in projects %}
        <article class="col-6 col-md-4 col-lg-3">
          <div class="card mb-3 secondBg">
            <div class="card-header text-center">Pour le : {{project.dueDate | date("d/m/Y")}}</div>
            <div class="card-body">
              <h5 class="card-title">{{project.name}}</h5>
              <p class="card-text">{{project.description}}</p>
            </div>
            <div class="card-footer">
              <a class="btn btn-secondary" href="{{ path('project_show', {'id': project.id}) }}">Voir</a>
              <a class="btn btn-secondary" href="{{ path('project_edit', {'id': project.id}) }}">Modifier</a>
               {{ include('project/_delete_form.html.twig') }}
            </div>
          </div>
        </article>
      {% endfor %}
      <article class="col-6 col-md-4 col-lg-3">
        <div class="card mb-3 secondBg text-center">
          <div class="card-header">Nouveau projet</div>
          <div class="card-body">
            <h5 class="card-title">
              <a class="btn btn-lg btn-secondary"href="{{ path('project_new') }}">+</a>
            </h5>
          </div>
          <div class="card-footer">
          </div>
        </div>
      </article>
    </section>
{% endblock %}
